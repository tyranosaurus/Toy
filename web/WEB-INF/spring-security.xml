<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                                 http://www.springframework.org/schema/beans/spring-beans.xsd
                                 http://www.springframework.org/schema/security
                                 http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <!-- CSRF -->
        <csrf disabled="true"/>

        <intercept-url pattern="/board/**" access="hasRole('ROLE_USER')" /> <!-- hasAnyRole(ROLE_USER, ROLE_ADMIN) -->
        <intercept-url pattern="/login/loginForm" access="hasRole('ROLE_ANONYMOUS')" />
        <!--<intercept-url pattern="/**" access="hasRole('ROLE_USER')"/>-->

        <form-login
            username-parameter="id"
            password-parameter="password"
            login-page="/login/loginForm"
            default-target-url="/board/main"
            authentication-failure-url="/login/loginForm?fail=true" />
    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="admin" password="1234" authorities="ROLE_ADMIN"/>
                <user name="cyj0619" password="1234" authorities="ROLE_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>